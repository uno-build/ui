export const OCTANE_RENDERER_ID: "uno";
export namespace compilerConfig {
    namespace renderers {
        namespace registry {
            namespace uno {
                let module: string;
                let target: string;
                let server: string;
                let text: string;
            }
        }
        let rules: {
            include: string[];
            renderer: string;
        }[];
    }
}
